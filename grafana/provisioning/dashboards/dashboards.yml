apiVersion: 1

providers:
  - name: 'Kasa Monitor Dashboards'
    orgId: 1
    folder: 'Kasa Monitor'
    folderUid: kasa-monitor
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards